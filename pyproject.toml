[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "enigma-machine"
version = "0.1.0"
description = "A component-based Enigma I backend simulator"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["enigma_machine*"]
